/**
 * Block Editor Script Functionality
 *
 * The following blocks and scripts are compiled into a single asset and loaded into the block editor.
 *
 */

// Add dynamic blocks to a single compiled asset
import './blocks/dynamic/random-quote';

// Add static blocks to a single compiled asset
import './blocks/static/hero-banner';
